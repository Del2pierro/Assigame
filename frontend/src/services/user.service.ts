/**
 * @file user.service.ts
 * @layer Service — Domaine Utilisateur
 * @role  Couche HTTP pour la gestion du profil utilisateur.
 *        Utilise l'instance api depuis api.client.ts.
 *
 * @endpoints
 *  - getProfile()    → GET /api/utilisateurs/{id}
 *                      Récupère les informations complètes d'un vendeur
 *
 *  - updateProfile() → PUT /api/utilisateurs/{id}
 *                      Met à jour les informations personnelles (nom, prénom,
 *                      téléphone, adresse)
 *
 * ⚠️ Ce fichier ne doit contenir AUCUN état ni logique UI.
 */

// Implémentation à venir
