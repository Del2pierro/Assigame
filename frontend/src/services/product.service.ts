/**
 * @file product.service.ts
 * @layer Service — Domaine Produits & Catégories
 * @role  Couche HTTP pour toutes les opérations sur les produits et catégories.
 *        Utilise l'instance api depuis api.client.ts.
 *
 * @endpoints
 *  - getAll()         → GET    /api/produits
 *  - getById()        → GET    /api/produits/{id}
 *  - getByCategory()  → GET    /api/produits/categorie/{idCategorie}
 *  - getBySeller()    → GET    /api/produits/utilisateur/{idUtilisateur}
 *  - create()         → POST   /api/produits/utilisateur/{uid}/categorie/{cid}
 *  - update()         → PUT    /api/produits/{id}
 *  - patchStatus()    → PATCH  /api/produits/{id}/statut/{nouveauStatut}
 *  - remove()         → DELETE /api/produits/{id}
 *  - getCategories()  → GET    /api/categories
 *
 * ⚠️ Ce fichier ne doit contenir AUCUN état ni logique UI.
 */

// Implémentation à venir
