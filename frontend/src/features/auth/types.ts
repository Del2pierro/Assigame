/**
 * @file types.ts
 * @feature auth
 * @role  Types TypeScript spécifiques au domaine authentification.
 *
 * @exports
 *  - LoginPayload         : { login: string; motDePasse: string }
 *  - RegisterPayload      : { nom, prenom, email, login, motDePasse, telephone?, adresse?, idTypeUtilisateur }
 *  - AuthResponse         : Profil vendeur retourné après connexion réussie (= Utilisateur)
 *  - TypeUtilisateur      : { idTypeUtilisateur: number; libelle: string }
 */

// Implémentation à venir
