/**
 * @file page.tsx
 * @route  /profil
 * @role   Page du profil du vendeur connecté. Affiche les informations du compte
 *         et permet leur modification via un formulaire inline.
 *         Assemble : ProfileDisplay + ProfileForm.
 *         Route PROTÉGÉE (authentification requise).
 */

export default function ProfilPage() {
  return null; // À implémenter — importer ProfileDisplay + ProfileForm
}
