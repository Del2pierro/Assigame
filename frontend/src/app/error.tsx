/**
 * @file error.tsx
 * @route  Toutes les routes (error boundary global)
 * @role   Intercepte les erreurs React non gérées au niveau de l'app.
 *         Affiche un message générique et propose de réessayer via reset().
 *         DOIT être un Client Component ("use client").
 */
"use client";

export default function GlobalError() {
  return null; // À implémenter
}
