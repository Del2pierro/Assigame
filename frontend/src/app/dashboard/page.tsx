/**
 * @file page.tsx
 * @route  /dashboard
 * @role   Page principale de l'espace vendeur. Affiche les statistiques rapides
 *         (articles en vente, articles vendus) et la liste des articles du vendeur
 *         avec leurs actions (modifier, supprimer, changer le statut).
 *         Assemble : StatsCards + SellerProductTable.
 *         Route PROTÉGÉE (authentification requise via DashboardLayout).
 */

export default function DashboardPage() {
  return null; // À implémenter — importer StatsCards + SellerProductTable
}
