/**
 * @file page.tsx
 * @route  /dashboard/articles/:id
 * @role   Page d'édition d'un article existant. Récupère l'ID depuis params.id
 *         et monte le composant ProductForm en mode "édition" avec les données
 *         pré-remplies de l'article concerné.
 *         Après soumission réussie, redirige vers /dashboard.
 *         Route PROTÉGÉE.
 */

export default function EditArticlePage() {
  return null; // À implémenter — importer ProductForm (mode édition)
}
