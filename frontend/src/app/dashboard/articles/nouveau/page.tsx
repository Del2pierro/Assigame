/**
 * @file page.tsx
 * @route  /dashboard/articles/nouveau
 * @role   Page de création d'un nouvel article. Monte le composant ProductForm
 *         en mode "création" (aucun ID pré-rempli).
 *         Après soumission réussie, redirige vers /dashboard.
 *         Route PROTÉGÉE.
 */

export default function NewArticlePage() {
  return null; // À implémenter — importer ProductForm (mode création)
}
