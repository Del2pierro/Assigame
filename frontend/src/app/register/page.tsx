/**
 * @file page.tsx
 * @route  /register
 * @role   Page d'inscription d'un nouveau vendeur.
 *         Assemble uniquement le composant RegisterForm de la feature auth.
 */

export default function RegisterPage() {
  return null; // À implémenter — importer RegisterForm depuis features/auth
}
