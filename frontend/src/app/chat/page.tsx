/**
 * @file page.tsx
 * @route  /chat
 * @role   Page du centre de messagerie. Lit searchParams.id pour pré-sélectionner
 *         une conversation. Assemble ConversationList et MessageThread.
 *         Accessible aux vendeurs connectés ET aux acheteurs invités (guest).
 */

export default function ChatPage() {
  return null; // À implémenter — importer ConversationList + MessageThread
}
