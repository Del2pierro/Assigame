/**
 * @file common.types.ts
 * @layer Types — Utilitaires génériques
 * @role  Types TypeScript réutilisables non liés à un domaine métier spécifique.
 *
 * @exports
 *  - Nullable<T>       : T | null
 *  - Optional<T>       : T | undefined
 *  - ID                : number (alias sémantique pour les identifiants)
 *  - UUID              : string (alias pour les UUIDs guest)
 *  - SortDirection     : 'asc' | 'desc'
 *  - SortField         : 'prix' | 'date' (champs de tri du catalogue)
 *  - PageParams        : { page: number; size: number }
 *  - ComponentWithChildren : { children: React.ReactNode }
 */

// Implémentation à venir
