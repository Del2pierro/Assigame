/**
 * @file constants.ts
 * @layer Lib — Utilitaires
 * @role  Constantes globales immuables partagées dans toute l'application.
 *        Centralise les valeurs "magic strings" pour éviter la duplication.
 *
 * @exports
 *  - PRODUCT_STATUS   : Enum des statuts produits (DISPONIBLE, RESERVE, VENDU)
 *  - ROUTES           : Map des routes nommées (évite les strings hardcodées)
 *  - LOCAL_STORAGE_KEYS : Clés localStorage standardisées
 *  - WS_ENDPOINTS     : Endpoints WebSocket STOMP (/ws, /app/chat.send, /topic/...)
 *  - PAGINATION       : Valeurs par défaut (taille de page, etc.)
 */

// Implémentation à venir
