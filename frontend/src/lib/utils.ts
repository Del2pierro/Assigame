/**
 * @file utils.ts
 * @layer Lib — Utilitaires
 * @role  Fonctions utilitaires pures sans dépendance React ni état.
 *        Chaque fonction est testable unitairement de façon isolée.
 *
 * @exports
 *  - formatPrice()      : Formate un nombre en devise (ex: 15 → "15,00 €")
 *  - formatDate()       : Formate une date ISO en format lisible (ex: "12 juin 2025")
 *  - toBase64()         : Convertit un File image en chaîne Base64 sans préfixe MIME
 *                         (utilisé dans ProductForm pour l'upload d'image)
 *  - generateGuestId()  : Génère un UUID préfixé "guest-" pour les acheteurs anonymes
 *  - truncateText()     : Tronque un texte à N caractères avec ellipsis
 */

// Implémentation à venir
