/**
 * @file useMediaQuery.ts
 * @layer Hooks — Utilitaire
 * @role  Hook pour détecter les breakpoints CSS depuis JavaScript.
 *        Permet d'adapter le comportement des composants en fonction
 *        de la taille d'écran (ex: layout split-panel du chat sur desktop
 *        vs vue unique sur mobile).
 *
 * @usage  const isDesktop = useMediaQuery('(min-width: 1024px)')
 *
 * @param  query — Media query CSS standard
 * @returns Booléen indiquant si la media query correspond
 */

// Implémentation à venir
