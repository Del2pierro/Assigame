/**
 * @file useDebounce.ts
 * @layer Hooks — Utilitaire
 * @role  Hook de debounce pour retarder l'exécution d'un effet après
 *        un délai configurable. Utilisé principalement pour la barre de
 *        recherche d'articles afin de limiter les appels API lors de la frappe.
 *
 * @usage  const debouncedSearch = useDebounce(searchTerm, 400)
 *         // debouncedSearch se met à jour 400ms après le dernier changement
 *
 * @param  value  — La valeur à surveiller
 * @param  delay  — Délai en millisecondes (recommandé : 300–500ms)
 * @returns La valeur stabilisée après le délai
 */

// Implémentation à venir
